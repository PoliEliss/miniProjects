<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rorono.a0603thefirstproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.0603TheFirstProject">
        <activity
            android:name=".ProjectOneCounter.Counter"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ProjectOneCounter.Primer2"
            android:exported="true">

        </activity>
        <activity android:name=".ProjectOneCounter.Calculate"
            android:exported="true">

        </activity>

    </application>

</manifest>